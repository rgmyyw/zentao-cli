# 开发上下文

聚合一个需求或 Bug 的开发上下文：自身信息、关联 Bug、用例、任务、动态摘要。仅支持 `entityType=story|bug`，不接收 `executionId`。

## 命令列表

| 命令 | 简介 |
| --- | --- |
| `getDevelopmentContext` | 可选。story 上下文查询关联 Bug 时可用于兜底过滤。 |
| `getDevelopmentContextSnapshot` | 可选。story 上下文查询关联 Bug 时可用于兜底过滤。 |
