# 构建高级操作

构建通知、指派、删除等低频操作。日常主链路见 `build.md`。

## 命令列表

| 命令 | 简介 |
| --- | --- |
| `notifyBuildBug` | 要通知的 Bug ID 列表，对应 18.5 build/notifyBug 页面 bugs[] 字段 |
| `assignBuildTo` | - |
| `deleteBuild` | - |

> 写操作必须传 `--confirm true`。完整参数以 `zentao help <command>` 输出为准。